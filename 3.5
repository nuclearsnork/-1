#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,n,c;
    scanf("%d", &n);
    c=0;
    for(i=2;i<n;i++)
    {
           if (n%i==0)
           {
               printf("no");
               return;
           }
    }
    printf("yes");
    return 0;
}
