#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i,n,a,b;
    scanf("%d", &n);
    scanf("%d", &b);
    n=n-1;
    for(i=0;i<n;i++)
    {
           scanf("%d", &a);
           if (a>b) b=a;
    }
    printf("%d\n", b);
    return 0;
}
